version: '3.9'
services:
  postgres:
    image: postgres:15.2
    environment:
      POSTGRES_DB: "mix_ai_db"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"